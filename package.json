{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/markdown-it": "^13.0.1",
    "@types/node": "^20.6.0",
    "markdown-it": "^13.0.1",
    "nuxt": "^3.2.2",
    "nuxt-icon": "^0.5.0"
  }
}
